# seo-agents helper scripts — dependencies

# Required
beautifulsoup4>=4.12.0,<5.0.0
requests>=2.32.4,<3.0.0
urllib3>=2.6.3,<3.0.0
playwright>=1.59.0,<2.0.0   # render_page, capture_screenshot (run: playwright install chromium)
Pillow>=12.1.0,<13.0.0      # capture_screenshot

# Optional — scripts degrade gracefully without these
lxml>=6.0.2,<7.0.0          # faster HTML parsing backend for parse_html
trafilatura>=2.0.0,<3.0.0   # main-content extraction in render_page
htmldate>=1.9.0,<2.0.0      # publication-date detection in render_page
